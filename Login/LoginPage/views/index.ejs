<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover">
    <link rel="stylesheet" href="/stylesheets/pico.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Flight Schedule Sign In</title>
    
    <style>
      /* Global CSS variables */
      :root {
        --spacing: 3rem;
        --text-weight: 400;
        --titles-weight: 800;
        --block-round: 0;
      }

      /* Typography */
      h2, h3, hgroup>:last-child {
        font-weight: 200;
      }
      small {
        color: var(--muted-text);
      }

      /* Header */
      
      
      
      
      
      :root:not([data-theme=dark]), [data-theme=light] {
          --nav-background: rgba(255, 255, 255, 0.7);
          --nav-border: rgba(115, 130, 140, 0.2);
      }

      [data-theme=dark] {
          --nav-background: rgba(16, 24, 30, 0.8);
          --nav-border: rgba(115, 130, 140, 0.2);
      }
    </style>
  </head>

  <body>
    <%- include('header'); -%>
    
    <div class="hero">
      <header class="container">
        <hgroup>
          <h1>Hello</h1>
          <h2>Sign in with your twitter account</h2>
        </hgroup>
        <p><a href="/login" role="button">Sign in</a></p>
      </header>
    </div>
    
    <main class="container">
      
      
      <section>
        <div class="grid">
          <div>
            <h4>Login with twitter</h4>
            <p>Get access to your application</p>
          </div>>
        </div>
      </section>
    </main>
    
    <footer class="container">
      <small>A <a href="https://www.passportjs.org/">Passport.js</a> example • <a href="https://github.com/passport/express-4.x-facebook-example">Source code</a></small>
    </footer>
  </body>
</html>
